---
import { siteConfig } from "@/site-config";

const year = new Date().getFullYear();
---

<footer class="border-b">
	<div
		class="mx-5 border-x border-l border-r px-0 py-8 text-sm font-normal sm:mx-24 sm:flex-row sm:justify-between sm:text-xs"
	>
		<div class="me-0 sm:me-4">
			&copy; {siteConfig.author}
			{year}
		</div>
		<!-- <nav
			aria-label="More on this site"
			class="flex gap-x-2 sm:gap-x-0 sm:divide-x sm:divide-gray-500"
		>
			{
				socialLinks.map(({ friendlyName, isWebmention, link, name }) => (
					<a
						class="flex items-center px-4 py-2 sm:py-0 sm:hover:text-textColor sm:hover:underline"
						href={link}
						target="_blank"
					>
						<div class="full flex h-7 w-7 items-center justify-start rounded-full">
							<Icon aria-hidden="true" class="h-5 w-5" focusable="false" name={name} />
						</div>
						{friendlyName}
					</a>
				))
			}
		</nav> -->
	</div>
</footer>
