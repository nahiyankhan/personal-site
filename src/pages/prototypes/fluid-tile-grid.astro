---
import PrototypeLayout from "@/layouts/PrototypeLayout.astro";
---

<PrototypeLayout
	meta={{ title: "Fluid Tile Grid" }}
	title="Fluid Tile Grid"
	demoHeight="70vh"
	demoStyle="immersive"
>
	<div slot="demo" class="demo-wrapper">
		<video autoplay loop muted playsinline class="demo-video">
			<source src="/videos/grid-loading.mp4" type="video/mp4" />
		</video>
	</div>

	<!-- Content section -->
	<article class="prototype-content">
		<h2>About</h2>
		<p>
			A prototype of a fluid, animated grid layout. Tiles smoothly reflow as items are added,
			removed, resized, or reordered—creating a dynamic, responsive canvas.
		</p>

		<h2>Technical Details</h2>
		<p>Built with React and Framer Motion for performant, physics-based animations.</p>
		<ul>
			<li><strong>React:</strong> Component-based architecture for modular tile management</li>
			<li><strong>Framer Motion:</strong> Smooth layout transitions with spring physics</li>
			<li><strong>CSS Grid:</strong> Fluid, responsive grid foundation</li>
		</ul>

		<h2>Features</h2>

		<div class="features-grid">
			<div class="feature-item">
				<video autoplay loop muted playsinline class="feature-video">
					<source src="/videos/grid-tiles-dnd.mp4" type="video/mp4" />
				</video>
				<div class="feature-label">
					<strong>Drag & Drop</strong>
					<span>Reorder tiles with smooth animated transitions</span>
				</div>
			</div>

			<div class="feature-item">
				<video autoplay loop muted playsinline class="feature-video">
					<source src="/videos/grid-tiles-resize.mp4" type="video/mp4" />
				</video>
				<div class="feature-label">
					<strong>Resize</strong>
					<span>Tiles dynamically resize and neighbors reflow</span>
				</div>
			</div>

			<div class="feature-item">
				<video autoplay loop muted playsinline class="feature-video">
					<source src="/videos/grid-tiles-data.mp4" type="video/mp4" />
				</video>
				<div class="feature-label">
					<strong>Data Updates</strong>
					<span>Real-time content updates within tiles</span>
				</div>
			</div>

			<div class="feature-item">
				<video autoplay loop muted playsinline class="feature-video">
					<source src="/videos/grid-tiles-infinite.mp4" type="video/mp4" />
				</video>
				<div class="feature-label">
					<strong>Infinite Scroll</strong>
					<span>Virtualized loading as you scroll</span>
				</div>
			</div>
		</div>
	</article>
</PrototypeLayout>

<style>
	.demo-wrapper {
		position: relative;
		width: 100%;
		height: 100%;
		min-height: 70vh;
		display: flex;
		align-items: center;
		justify-content: center;
		overflow: hidden;
	}

	.demo-video {
		width: 100%;
		height: 100%;
		object-fit: cover;
	}

	.prototype-content {
		color: hsl(var(--theme-text));
	}

	.prototype-content h2 {
		font-size: 1.5rem;
		font-weight: 700;
		margin-top: 2rem;
		margin-bottom: 1rem;
	}

	.prototype-content h2:first-child {
		margin-top: 0;
	}

	.prototype-content p {
		margin-bottom: 1rem;
		line-height: 1.75;
	}

	.prototype-content ul {
		margin-bottom: 1rem;
		padding-left: 1.5rem;
		list-style-type: disc;
	}

	.prototype-content li {
		margin-bottom: 0.5rem;
		line-height: 1.75;
	}

	/* Features grid */
	.features-grid {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 1.5rem;
		margin-top: 1.5rem;
	}

	.feature-item {
		border: 1px solid hsl(var(--border-divider) / 0.3);
		border-radius: 0.75rem;
		overflow: hidden;
		background: hsl(var(--theme-bg-inverse) / 0.03);
		transition: border-color 0.2s ease;
	}

	.feature-item:hover {
		border-color: hsl(var(--border-divider) / 0.5);
	}

	.feature-video {
		width: 100%;
		aspect-ratio: 1 / 1;
		object-fit: cover;
		display: block;
		background: hsl(var(--theme-bg-inverse) / 0.1);
	}

	.feature-label {
		padding: 1rem 1.25rem;
		display: flex;
		flex-direction: column;
		gap: 0.25rem;
	}

	.feature-label strong {
		font-size: 0.9375rem;
		font-weight: 600;
	}

	.feature-label span {
		font-size: 0.8125rem;
		opacity: 0.7;
		line-height: 1.4;
	}

	@media (max-width: 640px) {
		.demo-wrapper {
			min-height: 50vh;
		}

		.features-grid {
			grid-template-columns: 1fr;
		}
	}
</style>
